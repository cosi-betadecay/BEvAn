# Scoreboard V5 — Four-feature model: blind LOR score alongside the legacy angle

Loop surface: Phase 0 (iter 0) = wire `annihilation_lor_score` (iter-9 module
state, unmodified) as a FOURTH feature next to `delta_E`,
`annihilation_angle`, `arm` — `dataset/datasets.py` (raw-hit call site +
threading), `pipeline/train.py` (ΔE×lor joint, log/log, `log_x_floor=0.1`,
`log_y_floor=1e-3`, 25×25), `pipeline/eval.py` (lor lookups),
`modeling/calculate_probablities.py` (lor term in `R`), `run.py` (threading).
Phase 1 (iter ≥1) = `physics/annihilation_lor.py` plus the Phase-0 lines
themselves when the hypothesis is literally about them (binning, term form).
The legacy three features and `event_processing.py` are frozen.

**Success bar: beat MCC 0.8792 (V1–V3 champion, commit `342e417`,
575 / 116 / 28 / 5876).** The scoreboard "best" starts at 0.8792 — nothing is
committed until V5 beats it.

**Baseline recall = 0.9536. Guardrail floor = 0.9036** (recall ≥ baseline − 0.05).
Stop if recall < 0.9036 for two consecutive iters.

MCC = (TP·TN − FP·FN) / sqrt((TP+FP)(TP+FN)(TN+FP)(TN+FN)), computed from the
eval confusion matrix printed by `predict`.

**Harness pre-flight** (per iter, before the pipeline eval):
`pytest tests/tests_specialized/test_annihilation_lor.py --no-cov -q` — all 3
must pass; TV and axis median recorded below. **TV is a gate, never a
ranking** (four documented TV/MCC inversions in V4). Gate + axis + census
only; MCC decides keep/revert.

NaN convention (frozen): NaN lor (n<2 or n>12) → density 0 both classes →
term ratio 1 → the other three features decide. The score's zero point-mass
stays inside the bottom log bin (~6:1 bg-ward evidence).

| Run ID | Timestamp (UTC) | Status | Hypothesis | What changed | Harness TV | Harness axis med (deg) | MCC | Δ vs best | F1 | Precision | Recall | FPR | TP / FP / FN / TN | Notes / next idea |
|--------|-----------------|--------|------------|--------------|-----------|------------------------|-----|-----------|----|-----------|--------|-----|--------------------|-------------------|
| 0 | 2026-06-10T03:47:00Z | below bar | Phase 0 four-feature wiring: LOR score (iter-9 module state, unmodified) added next to the legacy three; densities decide per event | `datasets.py`: raw-hit `annihilation_lor_score` call + lor tensors threaded; `train.py`: ΔE×lor joint (log/log, x-floor 0.1, y-floor 1e-3, 25×25) + conditional; `eval.py`: lor lookups both branches; `calculate_probablities.py`: optional lor term in `R`; `run.py`: threading | 0.387 (carried) | 8.8 (carried) | 0.8788 | -0.0004 | 0.8877 | 0.8229 | 0.9635 | 0.0209 | 581 / 125 / 22 / 5867 | **0.0004 below the bar on the FIRST measurement — the four-feature premise holds: TP +6 / FN −6 vs champion (575/116/28/5876) at FP +9.** The LOR term converts 6 champion-FNs into TPs (recall 0.9536→0.9635, best non-sentinel recall yet) but admits 9 extra FP — exactly the trade the zero-bin 6:1 evidence prices. Train recall 0.9726: some density mass is train-fit (V1 lesson), so binning robustness is the right first lever. NaN excluded 0 (lor NaN passthrough working: ratio-1, no phantom entries). Harness 3/3, TV/axis carried (module untouched). Next (iter 1): action #1 lor-axis binning, `log_y_floor` 1e-3→1e-4 — purify the zero point-mass bin (finite scores in (1e-4,1e-3) leave it; exact zeros stay) to sharpen the degenerate-mass evidence. |
| 1 | 2026-06-10T03:55:00Z | reverted | action #1 lor-axis binning, point 1: `log_y_floor` 1e-3→1e-4 (purer zero bin; finite (1e-4,1e-3) scores leave the bottom bin) | `train.py` ΔE×lor block: `log_y_floor=1e-4` (one constant) | 0.387 (carried) | 8.8 (carried) | 0.8771 | -0.0021 | 0.8862 | 0.8215 | 0.9619 | 0.0210 | 580 / 126 / 23 / 5866 | **WORSE than iter 0: TP −1, FP +1.** Purifying the zero bin removes near-zero SIGNAL mass from the high-evidence bottom bin more than it sharpens the bg evidence — the (1e-4,1e-3) band is apparently signal-leaning, consistent with σ_cos=0.02 inflation pulling real signal just above exact 0. Reverted. Next (iter 2): the opposite point, `log_y_floor` 1e-3→1e-2 — widen the bottom bin (the (1e-3,1e-2) band joins the zero mass). |
| 2 | 2026-06-10T04:10:00Z | reverted | action #1 lor-axis binning, point 2: `log_y_floor` 1e-3→1e-2 (wider bottom bin; (1e-3,1e-2) band joins the zero mass) | `train.py` ΔE×lor block: `log_y_floor=1e-2` (one constant) | 0.387 (carried) | 8.8 (carried) | 0.8781 | -0.0011 | 0.8870 | 0.8218 | 0.9635 | 0.0210 | 581 / 126 / 22 / 5866 | **WORSE than iter 0: FP +1 at constant TP — both floor directions lose; 1e-3 is locally optimal, floor axis CLOSED** (1e-4 dilutes the bottom bin's signal mass, 1e-2 dilutes its bg evidence; the V4 zero-bin diagnostic threshold was measured at exactly 1e-3, so the prior matched the optimum). Reverted to 1e-3. Next (iter 3): action #2, the sharpest model-side hypothesis — replace the ΔE×lor JOINT term with the 1D marginal P(lor) ratio (same `lor_bins`, zero mass stays in the bottom bin): the score is ΔE-orthogonal by construction (V4 iter 3) and 25-bin conditionals are sparse (V4 iter 5), so the marginal may be denser AND unbiased. |
| 3 | 2026-06-10T04:25:00Z | reverted | action #2 marginal term form: lor term = 1D P(lor\|β)/P(lor\|bg) ratio (same `lor_bins`, smoothing 0) instead of the ΔE×lor joint | `train.py`: per-class `build_density_matrix_1d` over lor; `eval.py`: 1D lookups replace the joint lookups | 0.387 (carried) | 8.8 (carried) | 0.8628 | -0.0160 | 0.8746 | 0.8291 | 0.9254 | 0.0192 | 558 / 115 / 45 / 5877 | **MUCH WORSE (−0.0160): TP −23 / FN +23, FP −10. Action #2 REFUTED — the joint's implicit ΔE conditioning is protective, not redundant:** under the marginal, far-tail lor scores (partial-deposit signal: bg median 18 < signal median 152, the documented non-monotonicity) are charged bg-ward across ALL ΔE; the joint had localized that penalty to ΔE bins where bg actually lives. Echo of V4 iter 5: removing a ΔE vote starves recall. Notable: FP 115 < champion's 116 — the marginal IS more bg-discriminating, but at 2.3 TP per FP, far above the ~6:1 TP-worth. Reverted. Next (iter 4): action #3 feature-side — joint selection of (first_a, first_b) WITH internal residuals (iter 9 grades internals only at the first-scatter argmin; let them pick (i,k) too). Module change → full harness pre-flight. |
| 4 | 2026-06-10T05:10:00Z | reverted | action #3a joint (first_a, first_b) selection WITH internal residuals: every finite (i,k) candidate charged its internal walk before the argmin (V4 iter 9 graded only at the first-scatter argmin) | `annihilation_lor` bipartition loop: per-(i,k) internal evaluation with partial-sum pruning | 0.412 | 8.8 | 0.8765 | -0.0023 | 0.8858 | 0.8234 | 0.9585 | 0.0207 | 578 / 124 / 25 / 5868 | **WORSE than iter 0: TP −3, FP −1 — and the FIFTH TV/MCC inversion (TV .387→.412, MCC down).** Harness gate 3/3, axis held 8.8°, census unchanged (43/28,077; zero bin 6.7%/41.6%) but runtime 60s→240s. Letting internals veto the first-pair re-picks (i,k) on misordered-but-internally-consistent walks for ~Doppler-broadened signal — the V4 iter-9 asymmetry (internals vote on the PARTITION, firsts picked by first-scatter terms) was load-bearing, not an oversight. Module reverted to iter-9 state (`git checkout`). Action #3b (n>12 greedy bipartition) SKIPPED without a run: the NaN census 43/28,077 is byte-identical to V4's n<2 population, so the n>12 tail is empty in this file — nothing to recover. Next (iter 5): action #1's remaining probe — `n_bins_y` 25→12 for the lor matrix only (halve the lor axis: 625→300 joint cells; iter 3 proved the conditional structure carries the value, so densify it instead of marginalizing it). |
| 5 | 2026-06-10T05:35:00Z | reverted | action #1 optional probe: lor-axis resolution halved, `n_bins_y` 25→12 (densify the joint's conditional structure without marginalizing) | `train.py` ΔE×lor block: `n_bins_y=12` (one constant) | 0.387 (carried) | 8.8 (carried) | 0.8781 | -0.0007 | 0.8870 | 0.8218 | 0.9635 | 0.0210 | 581 / 126 / 22 / 5866 | **WORSE: FP +1 at constant TP (confusion matrix identical to iter 2). Action #1 fully CLOSED** — floor up/down, resolution down all lose to the Phase-0 defaults (25 bins, 1e-3 floor); the lor density model was born at its local optimum. Reverted to 25. Next (iter 6): seed #4 degenerate-mass split, the sanctioned feature-side relabel — graded (n≥3) hypotheses floored at 2e-3 (just above the bottom-bin boundary), structural dof=0 zeros (n=2, two single-hit chains) keep exactly 0, so the zero bin holds true degenerates alone. Module change → full pre-flight + census check. |
| 6 | 2026-06-10T06:05:00Z | reverted | seed #4 degenerate-mass split: graded (n≥3) scores floored at `MIN_GRADED_SCORE=2e-3` (out of the bottom bin); n=2 structural zeros keep exactly 0 | `annihilation_lor`: `score = geo_resid if n == 2 else max(geo_resid, 2e-3)` + constant | 0.391 | 8.8 | 0.8761 | -0.0027 | 0.8853 | 0.8213 | 0.9602 | 0.0210 | 579 / 126 / 24 / 5866 | **WORSE: TP −2, FP +1 vs iter 0.** Pre-flight clean (3/3, axis 8.8°, NaN census unchanged; zero bin sig 6.7%→5.7%, bg 41.6%→41.2% — evidence sharpened 6.2:1→7.2:1 exactly as designed) yet MCC fell: the ~1% of signal with graded near-zero scores NEEDED the zero bin's strong signal-ward... no — bg-ward bin, yet moving them out repriced them worse. Echo of iter 1: every attempt to purify the zero bin (boundary down, sentinel out) loses — the mixture is load-bearing. Seed #4 closed; module reverted. Next (iter 7): seed #5, the label-edge diagnostic's lever — minimum-lever-arm penalty: graded (n≥3) reported scores += (σ_pos/d_LOR)²/(2σ_cos²) with σ_pos=0.1 cm; sub-cm LOR baselines (direction-information-free geometric coincidences, 73% of annihilation-like bg) get pushed out of the annihilation-like range, multi-cm real LORs move negligibly. Selection + zero mass untouched → axis must hold. |
| 7 | 2026-06-10T06:40:00Z | reverted | seed #5 minimum-lever-arm penalty: graded scores += (σ_pos/d_LOR)²/(2σ_cos²), σ_pos = 0.1 cm (report-side; selection + n=2 zero mass untouched) | `annihilation_lor`: `SIGMA_POS_CM=0.1` + post-loop penalty term | 0.395 | 8.8 | 0.8774 | -0.0014 | 0.8863 | 0.8206 | 0.9635 | 0.0212 | 581 / 127 / 22 / 5865 | **WORSE: FP +2 at constant TP — the penalty backfired via the iter-4-V4 mechanism in miniature:** shifting graded bg mass upward in score drained bg density from the low-score bins, repricing the REMAINING low-score bg signal-ward (+2 FP). The sub-cm coincidences it targeted are mostly inside the untouchable n=2 zero mass anyway (73% no-ANNI ⊂ degenerate low-multiplicity). Seed #5 closed; σ_pos sweep not extended (stronger = more renormalization). Module reverted. All five policy-ranked actions now exhausted; 7 consecutive below iter 0. Next (iter 8): term form (sanctioned Phase-0-line hypothesis) — in double-count mode the lor JOINT gives ΔE a THIRD vote, and V3 proved the residual FP are precisely the 511-like-in-ΔE bg, so the third vote feeds them; switch the lor term to the conditional P(lor\|ΔE) ratio (already built in train.py) keeping ΔE at exactly the champion's two votes. |
| 8 | 2026-06-10T07:05:00Z | **NEW BEST — BAR BEATEN, committed** | term form: lor term = conditional P(lor\|ΔE) ratio instead of the joint — ΔE stays at exactly the champion's two votes (the joint's third ΔE vote was feeding the 511-like-in-ΔE FP class) | `eval.py` double-count branch: lor lookups read `bdecay_cond_lor`/`bg_cond_lor` (built since Phase 0) instead of the joints | 0.387 (carried) | 8.8 (carried) | **0.8823** | **+0.0031** | 0.8916 | 0.8360 | 0.9552 | 0.0189 | 576 / 113 / 27 / 5879 | **BEATS THE V1–V3 CHAMPION (0.8792): TP +1, FP −3, FN −1, TN +3 — strictly better on every cell.** Vs iter 0 (joint term): FP 125→113 (−12), TP 581→576 (−5) — net +0.0035. The diagnosis held: the joint multiplied in a third ΔE ratio, and since V3 proved the residual FP are energy-indistinguishable (511-like ΔE), that vote subsidized them; the conditional keeps only the lor's NEW information, localized per ΔE bin. The conditional's sparsity (V4 iter-5 worry) is priced correctly here because empty rows → 0 density both classes → ratio 1 (no evidence), unlike the marginal (iter 3) which globalized the non-monotone tail. Committed + pushed. Next (iter 9): stabilize the sparse conditional rows — Laplace smoothing on the lor joint pair (`smoothing=0.5`) so thin ΔE rows shrink toward uniform (ratio→1) instead of noisy hard evidence. |
| 9 | 2026-06-10T07:35:00Z | **NEW BEST — committed** | Laplace smoothing on the lor joint pair feeding the conditional: thin ΔE rows shrink toward uniform (ratio→1) instead of noisy hard evidence | `train.py` lor joints: `smoothing=0` → `0.5` (two lines, one constant) | 0.387 (carried) | 8.8 (carried) | **0.8841** | **+0.0018** | 0.8932 | 0.8374 | 0.9569 | 0.0187 | 577 / 112 / 26 / 5880 | **Better on every cell vs iter 8 (TP +1, FP −1, FN −1, TN +1) — and train MCC FELL (.8818→.8808) while eval rose: the smoothing bought generalization, not fit** (the V1 density-refit non-monotonicity finally pointing our way). Sparse-row noise was costing both sides; α=0.5 ≈ 12.5 pseudo-counts/row only moves thin rows. Committed + pushed. Next (iter 10): smoothing sweep point 2 — α=1.0 (the codebase's 1D-marginal convention); if it ties or loses, 0.5 stands per gentle-beats-hard. |
| 10 | 2026-06-10T08:00:00Z | reverted (tie) | smoothing sweep point 2: α 0.5→1.0 | `train.py` lor joints: `smoothing=1.0` | 0.387 (carried) | 8.8 (carried) | 0.8841 | ±0.0000 | 0.8932 | 0.8374 | 0.9569 | 0.0187 | 577 / 112 / 26 / 5880 | **Eval confusion matrix IDENTICAL to iter 9 — the smoothing plateau is flat between 0.5 and 1.0** (train MCC keeps drifting down, .8808→.8801: more shrinkage, same eval). The thin rows are already neutralized at 0.5; larger α only erodes fit. Axis closed; committed 0.5 stands. Next (iter 11): the one untested Phase-0 binning constant — lor pair's `log_x_floor` 0.1→1e-3 (align ΔE rows with the arm block's convention): under the CONDITIONAL term the ΔE row partition directly shapes P(lor\|ΔE); finer near-zero-ΔE rows separate the 511-clean population from the tails. |
| 11 | 2026-06-10T08:25:00Z | **NEW BEST — committed** | lor pair `log_x_floor` 0.1→1e-3: finer near-zero-ΔE rows for the conditional (align with the arm block's ΔE convention) | `train.py` ΔE×lor bins: `log_x_floor=1e-3` (one constant) | 0.387 (carried) | 8.8 (carried) | **0.8842** | **+0.0002** | 0.8936 | 0.8406 | 0.9536 | 0.0182 | 575 / 109 / 28 / 5883 | **Marginal but strict new best (0.884243 vs 0.884055): TP −2, FP −3.** Finer 511-clean rows sharpen the conditional exactly where the dangerous bg lives (V3: residual FP are 511-like in ΔE) — 3 more of them priced out at the cost of 2 borderline TP; recall lands exactly on the 0.9536 baseline (guardrail comfortable). Committed + pushed. Next (iter 12): same direction, lor pair `n_bins_x` 25→40 — more ΔE rows overall; smoothing (α=0.5) carries the added sparsity. |
| 12 | 2026-06-10T08:50:00Z | **NEW BEST — committed** | lor pair `n_bins_x` 25→40: finer ΔE conditioning rows across the range | `train.py` ΔE×lor bins: `n_bins_x=40` (one constant) | 0.387 (carried) | 8.8 (carried) | **0.8857** | **+0.0015** | 0.8949 | 0.8431 | 0.9536 | 0.0179 | 575 / 107 / 28 / 5885 | **FP −2 at constant TP vs iter 11 — FP now 107 vs the champion's 116 at identical TP/FN.** Train MCC keeps falling (.8801→.8794) while eval rises: the smoothed fine-row conditional is a regularized localizer, not a memorizer. The iter-8/9/11/12 stack (+0.0069 total) is one coherent mechanism: give the lor feature ONLY its conditional information, localized as tightly in ΔE as the data supports, with thin rows shrunk to neutrality. Committed + pushed. Next (iter 13): one more point on the axis — `n_bins_x` 40→60 (find the elbow). |
