# Scoreboard V2

Best so far: iter 2, MCC=0.8712

| Run ID | Timestamp (UTC) | Status | Hypothesis | What changed | MCC | Δ vs best | F1 | Precision | Recall | FPR | TP / FP / FN / TN | Notes / next idea |
|--------|-----------------|--------|------------|--------------|-----|-----------|----|-----------|--------|-----|--------------------|-------------------|
| 0 | 2026-06-09T14:48:00Z | baseline | — | post-Phase-0 clean pipeline (NaN-mask in confusion matrix + full-population class prior) | 0.8663 | — | 0.8773 | 0.8248 | 0.9370 | 0.0200 | 565 / 120 / 38 / 5872 | **V2 baseline. baseline_recall=0.9370, guardrail floor=0.8870.** PHASE-0 FINDING: both metric fixes are *no-ops on this data*. `eval - NaN excluded: 0` because `lookup_density_values` (matrix_calculations.py:138-155, OUTSIDE the writable set) already maps NaN/inf features→0.0 density, so NaN never reaches `ratio`/predictions. Prior fix also no-op: `delta_E` is finite for every n_hits≥1 event, so `isfinite(bdecay_train_delta_E).sum() == .numel()`. **Numbers are identical to the V1 baseline → RL_V2's "metric noise" premise does NOT manifest; V1's reverts were real, not NaN/prior artifacts.** The true non-monotonicity driver (per V1 iter6) is train-time density re-fitting reshaping the decision boundary, not NaN tallying. Phase 1 must rely on *augmentation* (lowering a feature's min) since removal is confirmed near-zero-sum. NaN-mask + true-population-prior fixes are kept as defensive correctness (protect other datasets). Third-bug logged (lookup swallows NaN), NOT fixed per policy. |
| 1 | 2026-06-09T14:56:00Z | kept | ARM-targeted SSD-ordering augmentation: for each subset size≥2, also emit the descending-energy ordering (E_1>E_2>…, Boggs & Jean §3) as an extra candidate so arm's argmin can reach a physics-consistent ordering | `event_processing.py` combo loop: append `tuple(sorted(c, key=energy, reverse=True))` per subset when it differs from ascending order; ≤2× combos; +5-line comment | 0.8677 | +0.0014 | 0.8787 | 0.8272 | 0.9370 | 0.0197 | 565 / 118 / 38 / 5874 | **NEW BEST — first non-reverted result across V1+V2.** FP 120→118 (−2), TN 5872→5874 (+2), **signal (TP=565, FN=38) untouched → recall unchanged 0.9370.** This is the FREE gain removal could never get: augmentation only ADDS candidates (lowers arm's min), and arm is the only order-sensitive (hence improvable) feature — delta_E/angle are order-invariant and already globally optimal over the enumerated pool. Train confirms same shape (FP 486→488, ~flat). Small (+0.0014, near V1's ~0.003 noise band) but positive, clean, and zero-recall-cost, so kept per protocol. Mechanism: the extra ordering reshapes the arm density fit so 2 background events fall the right side of the boundary. Next (iter2): escalate toward fuller CKD argmin — try all (first,second) orderings but CAP per-event combo count to avoid OOM on high-multiplicity events (C(n,7) blow-up). If capped-full-ordering beats SSD-single, the ordering lever has real headroom. |
| 2 | 2026-06-09T15:00:00Z | kept | escalate arm-ordering augmentation to FULL (first,second) CKD enumeration for short subsets (size≤4); keep iter-1 SSD pair for size≥5 to bound blow-up. Pool ⊇ iter1 so arm's min can only drop | `event_processing.py`: `_orderings(c)` helper — size≤4 emits all r(r-1)≤12 ordered (first,second) pairs (rest ascending), size≥5 keeps [ascending, descending-E] | 0.8712 | +0.0035 | 0.8814 | 0.8230 | 0.9486 | 0.0205 | 572 / 123 / 31 / 5869 | **NEW BEST, Δ+0.0035 (above V1's ~0.003 noise band).** Recall ROSE 0.9370→0.9486: TP 565→572, FN 38→31 — giving arm the full CKD ordering of short Compton tracks recovered 7 true signal events whose physical ordering the arbitrary ascending enumeration hid (arm's argmin found a low-arm consistent ordering). FP +5 (118→123) but TP +7 → MCC nets up. Train mirrors (TP 2312→2318, FN 99→93). **The ordering lever has real, signal-side headroom — augmentation lowers arm's min for events with a genuine consistent ordering (signal) more than for incoherent background (CKD principle, theory §2/§6 ranked #1).** Next (iter3): the size≥5 subsets still only get 2 orderings; the headroom may continue. Try extending full (first,second) enumeration to size≤5 OR add a per-event combo CAP and go full-ordering for ALL sizes under the cap — test whether large-subset orderings add more signal recovery. Watch FP creep; if FP grows faster than TP, ordering is exhausted. |
