# Scoreboard V2

Best so far: iter 7, MCC=0.8756

| Run ID | Timestamp (UTC) | Status | Hypothesis | What changed | MCC | Δ vs best | F1 | Precision | Recall | FPR | TP / FP / FN / TN | Notes / next idea |
|--------|-----------------|--------|------------|--------------|-----|-----------|----|-----------|--------|-----|--------------------|-------------------|
| 0 | 2026-06-09T14:48:00Z | baseline | — | post-Phase-0 clean pipeline (NaN-mask in confusion matrix + full-population class prior) | 0.8663 | — | 0.8773 | 0.8248 | 0.9370 | 0.0200 | 565 / 120 / 38 / 5872 | **V2 baseline. baseline_recall=0.9370, guardrail floor=0.8870.** PHASE-0 FINDING: both metric fixes are *no-ops on this data*. `eval - NaN excluded: 0` because `lookup_density_values` (matrix_calculations.py:138-155, OUTSIDE the writable set) already maps NaN/inf features→0.0 density, so NaN never reaches `ratio`/predictions. Prior fix also no-op: `delta_E` is finite for every n_hits≥1 event, so `isfinite(bdecay_train_delta_E).sum() == .numel()`. **Numbers are identical to the V1 baseline → RL_V2's "metric noise" premise does NOT manifest; V1's reverts were real, not NaN/prior artifacts.** The true non-monotonicity driver (per V1 iter6) is train-time density re-fitting reshaping the decision boundary, not NaN tallying. Phase 1 must rely on *augmentation* (lowering a feature's min) since removal is confirmed near-zero-sum. NaN-mask + true-population-prior fixes are kept as defensive correctness (protect other datasets). Third-bug logged (lookup swallows NaN), NOT fixed per policy. |
| 1 | 2026-06-09T14:56:00Z | kept | ARM-targeted SSD-ordering augmentation: for each subset size≥2, also emit the descending-energy ordering (E_1>E_2>…, Boggs & Jean §3) as an extra candidate so arm's argmin can reach a physics-consistent ordering | `event_processing.py` combo loop: append `tuple(sorted(c, key=energy, reverse=True))` per subset when it differs from ascending order; ≤2× combos; +5-line comment | 0.8677 | +0.0014 | 0.8787 | 0.8272 | 0.9370 | 0.0197 | 565 / 118 / 38 / 5874 | **NEW BEST — first non-reverted result across V1+V2.** FP 120→118 (−2), TN 5872→5874 (+2), **signal (TP=565, FN=38) untouched → recall unchanged 0.9370.** This is the FREE gain removal could never get: augmentation only ADDS candidates (lowers arm's min), and arm is the only order-sensitive (hence improvable) feature — delta_E/angle are order-invariant and already globally optimal over the enumerated pool. Train confirms same shape (FP 486→488, ~flat). Small (+0.0014, near V1's ~0.003 noise band) but positive, clean, and zero-recall-cost, so kept per protocol. Mechanism: the extra ordering reshapes the arm density fit so 2 background events fall the right side of the boundary. Next (iter2): escalate toward fuller CKD argmin — try all (first,second) orderings but CAP per-event combo count to avoid OOM on high-multiplicity events (C(n,7) blow-up). If capped-full-ordering beats SSD-single, the ordering lever has real headroom. |
| 2 | 2026-06-09T15:00:00Z | kept | escalate arm-ordering augmentation to FULL (first,second) CKD enumeration for short subsets (size≤4); keep iter-1 SSD pair for size≥5 to bound blow-up. Pool ⊇ iter1 so arm's min can only drop | `event_processing.py`: `_orderings(c)` helper — size≤4 emits all r(r-1)≤12 ordered (first,second) pairs (rest ascending), size≥5 keeps [ascending, descending-E] | 0.8712 | +0.0035 | 0.8814 | 0.8230 | 0.9486 | 0.0205 | 572 / 123 / 31 / 5869 | **NEW BEST, Δ+0.0035 (above V1's ~0.003 noise band).** Recall ROSE 0.9370→0.9486: TP 565→572, FN 38→31 — giving arm the full CKD ordering of short Compton tracks recovered 7 true signal events whose physical ordering the arbitrary ascending enumeration hid (arm's argmin found a low-arm consistent ordering). FP +5 (118→123) but TP +7 → MCC nets up. Train mirrors (TP 2312→2318, FN 99→93). **The ordering lever has real, signal-side headroom — augmentation lowers arm's min for events with a genuine consistent ordering (signal) more than for incoherent background (CKD principle, theory §2/§6 ranked #1).** Next (iter3): the size≥5 subsets still only get 2 orderings; the headroom may continue. Try extending full (first,second) enumeration to size≤5 OR add a per-event combo CAP and go full-ordering for ALL sizes under the cap — test whether large-subset orderings add more signal recovery. Watch FP creep; if FP grows faster than TP, ordering is exhausted. |
| 3 | 2026-06-09T15:05:00Z | reverted | full (first,second) ordering for ALL subset sizes with a 300k per-event combo cap (small-first, large subsets fall back to bounded pair) | `_full_orderings` + `_bounded_orderings` + per-event MAX_COMBOS cap | 0.8712 | 0.0000 | 0.8814 | 0.8230 | 0.9486 | 0.0205 | 572 / 123 / 31 / 5869 | **BYTE-IDENTICAL to iter2 (train AND eval) → ordering on size≥5 subsets is pure redundant cost.** The arm-minimizing ordering for every event always lives in a short (≤4-hit) Compton track — expected, since arm=\|theta_geo−theta_kin\| with theta_geo on 2 hits and the cleanest single-scatter consistency is a small-subset config. Also ~5× SLOWER (full ordering on size5-7 explodes combo volume) for zero gain. **Ordering lever EXHAUSTED at iter2 (size≤4 full is the optimum).** Reverted to iter2. Conclusion: for ≤7-hit events ALL augmentation is now tapped — angle/delta_E order-invariant & already globally optimal, arm-ordering fully captured. Remaining levers: (a) >7-hit candidate augmentation for the V1-signal-rich 8-9 band (the 7-cap may exclude the best 511/b2b subset); (b) removal on the augmented pool (augmentation now protects signal's arm, so an FP-targeting removal may finally pareto-improve — precision 0.823/FP=123 is the weak spot). Next (iter4): lever (a) — raise size cap to full enumeration for n_hits≤9 only (bounded: ≤10 extra subsets/event; keep 7-cap for FP-rich n≥10). |
| 4 | 2026-06-09T15:10:00Z | reverted | raise size cap to full enumeration for n_hits≤9 (signal-rich 8-9 band); keep 7-cap for n≥10 | `size_cap = (n_hits+1) if n_hits<=9 else 8` | 0.8712 | 0.0000 | 0.8814 | 0.8230 | 0.9486 | 0.0205 | 572 / 123 / 31 / 5869 | **BYTE-IDENTICAL to iter2 again → the 7-hit cap costs NO signal.** Size-8/9 subsets never improve any feature min: delta_E (target 511) is better served by a smaller subset summing near 511 (the 8/9-hit sum overshoots), and angle/arm favor small subsets. **AUGMENTATION CONCLUSIVELY EXHAUSTED** (iters 2,3,4): for ≤7-hit events angle/delta_E are order-invariant & globally optimal, arm-ordering captured at iter2, and larger subsets/more-orderings add nothing. 2 consecutive no-best. Reverted. **PIVOT to removal**, but only the ONE untried high-leverage kind: CKD χ² kinematic-consistency rejection (theory §2/§5 ranked #1, factor 3-6 discriminator; V1's removals were energy/lever-arm/n_hits, never CKD). Asymmetry that may beat V1's near-zero-sum: signal Compton tracks ARE kinematically consistent (geo scatter angle ≈ Compton-formula angle) → survive; background coincidences mimicking 511 are inconsistent → removed → their feature mins rise → fewer FP. Precision 0.823 / FP=123 is the bottleneck. Next (iter5): drop size≥3 subsets whose best-ordering CKD residual mean((cosφ_geo−cosφ_kin)²) > T=0.5; keep size≤2 (no empty events); b2b-exempt (min inter-hit vector cos ≤ −0.6, protects annihilation_angle). |
| 5 | 2026-06-09T15:16:00Z | reverted | CKD kinematic-consistency removal: drop size≥3 subsets whose best-of-3-orderings mean sq (cosφ_geo−cosφ_kin) residual > 0.5; size≤2 always kept; b2b-exempt | `event_processing.py`: `_ckd_residual`, `_is_back_to_back`, `_keep_subset` gate before `_orderings` | 0.8709 | -0.0003 | 0.8812 | 0.8240 | 0.9469 | 0.0204 | 571 / 122 / 32 / 5870 | **Near-zero-sum AGAIN (TP −1, FP −1).** But T=0.5 is so permissive it barely fired (only 1 event moved each way) → did NOT exercise the asymmetry. The CKD residual for inconsistent subsets is ~0.5-0.7, so T=0.5 drops only the very worst. Not conclusive. Reverted. Next (iter6): tighten to T=0.2 (drops moderate inconsistency, \|Δcos\|≳0.45) to actually test the slope — if signal/bg asymmetry is real, FP should fall faster than TP; if it stays −1:−1, CKD removal is confirmed zero-sum like all V1 removals and removal is fully exhausted. |
| 6 | 2026-06-09T15:22:00Z | reverted | CKD removal tightened to T=0.2 (exercise the filter) | `CKD_RESID_MAX 0.5→0.2` | 0.8696 | -0.0016 | 0.8806 | 0.8319 | 0.9353 | 0.0190 | 564 / 114 / 39 / 5878 | **Filter now fires: FP −9 (123→114) BUT TP −8 (572→564). ~1:1 trade → asymmetry REFUTED at subset level.** Two CKD points (T0.5→−1/−1, T0.2→−8/−9) give a consistent 1:1 slope; MCC is TP-sensitive (class imbalance) so tighter T only worsens. **SUBSET REMOVAL CONCLUSIVELY ZERO-SUM (energy, geometry, n_hits, CKD — all of V1 + iters 5,6).** ROOT CAUSE: dropping a subset also raises the order-invariant delta_E/angle mins, so each FP removed costs a TP. **FIX (iter7): filter ORDERINGS, not subsets.** Keep every subset (delta_E/angle untouched), but for arm emit only CKD-CONSISTENT orderings → background loses its spurious low-arm ordering while signal keeps its real consistent one. arm can only RISE vs iter2, and only for subsets whose arm-min ordering is inconsistent (≈background). Should cut iter2's residual FP with ~no TP cost. Next (iter7): in `_orderings`, for size 3-4 emit only orderings with CKD residual ≤ 0.2 (fallback: lowest-residual ordering so the subset still feeds delta_E/angle); size 2 keep both (no CKD redundancy). |
| 7 | 2026-06-09T15:27:00Z | kept | CKD-consistency ORDERING filter: keep every subset, but for size 3-4 feed arm only orderings with CKD residual ≤0.2 (fallback lowest-residual); size 2 keep both; size≥5 unchanged | `event_processing.py`: `_unit`,`_ckd_residual` helpers; `_orderings` prunes size 3-4 orderings by CKD consistency | 0.8756 | +0.0044 | 0.8853 | 0.8261 | 0.9536 | 0.0202 | 575 / 121 / 28 / 5871 | **NEW BEST, biggest jump yet (+0.0044), breaks the 4-iter no-best streak.** vs iter2: TP +3 (572→575) AND FP −2 (123→121) — BOTH directions improve. Filtering ORDERINGS (not subsets) is the key: delta_E/angle structurally untouched (every subset still emits ≥1 ordering), while arm sees only physically-consistent orderings. Removing spurious low-arm orderings from the TRAIN density fit sharpened the arm signal/bg boundary → recovered 3 signal events + cut 2 FP. This is the asymmetry subset-removal (iters 5,6: 1:1) could never capture, because removal raised delta_E/angle mins too. **Validated thesis: physics-consistency belongs on ORDERINGS, not subset membership.** Recall ROSE 0.9486→0.9536. Total V2 gain now +0.0093 over baseline (0.8663→0.8756). Next (iter8): sweep CKD_ORDER_MAX — try 0.1 (stricter, more spurious orderings pruned) and 0.35 (looser). T=0.2 was a first guess; the optimum may differ. Watch recall vs FP. |
