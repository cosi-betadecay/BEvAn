import torch


def radial_ordering(positions: torch.Tensor) -> bool:
    return


def radial_ordering_preprocessing(
    energies: torch.Tensor, positions: torch.Tensor
) -> tuple[torch.Tensor, torch.Tensor]:
    return
